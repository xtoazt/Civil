const http = require('http');
const https = require('https');

const stream = require('stream');

const fs = require('fs');
const path = require('path');

const name = require('testcafe-hammerhead/lib/utils/url');
const proxy = require('testcafe-hammerhead');

// WIP!